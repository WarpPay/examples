<!doctype html>
<html>

<head>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link href="https://fonts.googleapis.com/css?family=Pacifico&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Karla&display=swap');

        body {
            font-family: 'Karla', sans-serif;
        }

        h1 {
            font-family: Pacifico, sans-serif;
            font-size: 4em;
            color: #3eb5f1;
            margin: 0;
        }

        h2 {
            font-weight: 300;
            font-family: sans-serif;
        }

        .centered {
            top: 50%;
            left: 50%;
            text-align: center;
        }

        .container {
            max-width: 1335px;
            margin: 0 auto;
        }

        #content {
            margin-top: 50px;
        }

        #gh-banner {
            /* Positioning */
            position: fixed;
            top: 0;

            /* Sizing */
            height: 50px;
            width: 100vw;

            /* Layout for children */
            display: flex;
            flex-direction: row;
            justify-content: center;
            align-items: center;

            /* Styling */
            background-color: rgb(255, 250, 245);
            box-shadow: 0 4px 6px 0 hsla(0, 0%, 0%, 0.2);
        }

        .grid-row {
            display: flex;
            flex-flow: row wrap;
            justify-content: flex-start;
        }

        @media only screen and (max-width: 900px) {
            .grid-row {
                display: flex;
                flex-flow: column wrap;
                justify-content: flex-start;
            }
        }
    </style>
</head>

<body>
    <div id='gh-banner'>
        <img src="https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png" height=20 width=20 />
        <b>
            This Warp example is hosted <a href="https://www.github.com/warppay/examples">on GitHub</a>.
        </b>
    </div>

    <div id='content'>
        <div class="centered">
            <h1>My Example Shop</h1>
            <h2>This is a great place to buy lorems and ipsems.</h2>
        </div>

        <div class="container">
            {{#each items}}
            <div class="grid-row">
                <a href='/items/{{this.id}}'>
                    <img src="{{this.imageUrl}}" />
                    <h3>{{this.name}}</h3>
                </a>
            </div>
            {{/each}}
        </div>
    </div>
</body>

</html>